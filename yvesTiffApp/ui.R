library(shiny)

# Define UI for app that draws a histogram ----
ui <- fluidPage(
  
  titlePanel("Which Tiff & Yves hybrid unlocks your bucks?"),
  
  fluidRow(plotOutput(outputId = "origPlot",width="100%")),
  fluidRow(h4("Unlock your bucks by reconstructing our Tiff & Yves hybrid.
      Hybrids can be constructed by scrambling the first 30 eigenvectors of an SVD of original Tiff and Yves pics using the slider.")),  
  fluidRow(sliderInput(inputId = "seed",
                  label="",
                 min = 0,
                 max = 999,
                 value = 0, step=1)),
  fluidRow(span(textOutput("printCode"), style = "color:#428bca; font-size:15px; font-family:arial")),
  fluidRow(actionButton("minus", "-"),
      actionButton("plus", "+")),
  fluidRow(plotOutput(outputId = "searchPlot")),
  fluidRow(h6("Copyright on original images by Yves Gabriëls. The rds files and the images generated by this tool are not allowed to be used without permission of the copyright holder of the original images.")))
#  )